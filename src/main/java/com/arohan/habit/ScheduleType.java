package com.arohan.habit;

public enum ScheduleType {
    DAILY,
    SELECTED_WEEKDAYS,
    ALTERNATE_DAYS,
    EVERY_N_DAYS,
    TIMES_PER_WEEK,
    TIMES_PER_MONTH,
    ROTATION,
    ONE_TIME,
    CUSTOM
}
